@model IEnumerable<E3DBPI.Models.BlogCategory>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Post_CategoryName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Post_CategoryText)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Post_CategoryImage)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Post_Active)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Post_CategoryName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Post_CategoryText)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Post_CategoryImage)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Post_Active)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Post_CategoryID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Post_CategoryID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Post_CategoryID })
        </td>
    </tr>
}

</table>
